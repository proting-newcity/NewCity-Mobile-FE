import 'package:flutter/material.dart';

const Radius radiusSm = Radius.circular(5);
const Radius radiusMd = Radius.circular(8);
const Radius radiusLg = Radius.circular(10);
const Radius radiusXl = Radius.circular(16);
const Radius radiusXxl = Radius.circular(20);
const Radius radiusXxxl = Radius.circular(25);
const Radius radiusXxxxl = Radius.circular(30);

const BorderRadius borderSm = BorderRadius.all(radiusSm);
const BorderRadius borderMd = BorderRadius.all(radiusMd);
const BorderRadius borderLg = BorderRadius.all(radiusLg);
const BorderRadius borderXl = BorderRadius.all(radiusXl);
const BorderRadius borderXxl = BorderRadius.all(radiusXxl);
const BorderRadius borderXxxl = BorderRadius.all(radiusXxxl);
const BorderRadius borderXxxxl = BorderRadius.all(radiusXxxxl);

BorderRadius borderMdCircular = BorderRadius.circular(8);
BorderRadius borderLgCircular = BorderRadius.circular(10);
BorderRadius borderXlCircular = BorderRadius.circular(15);
BorderRadius borderXlAltCircular = BorderRadius.circular(16);
BorderRadius borderXxlCircular = BorderRadius.circular(20);
BorderRadius borderXxxxlCircular = BorderRadius.circular(30);
